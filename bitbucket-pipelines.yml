image: alpine/git:latest

pipelines:
  branches:
    main:
      - step:
          name: Mirror to GitHub
          clone:
            depth: full
          script:
            - git remote add github https://${GITHUB_TOKEN}@github.com/brownkw/ha-jira-integration.git
            - git push --mirror github
